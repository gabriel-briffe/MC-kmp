package org.mountaincircles.app.utils

import platform.Foundation.NSDecimalNumber
import kotlin.math.cos
import kotlin.math.sin

/**
 * iOS implementation of math utilities
 */
actual fun cos(angle: Double): Double = cos(angle)
actual fun sin(angle: Double): Double = sin(angle)
